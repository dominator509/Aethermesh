"""DID cache and revocation cache persistence (SQLite).

SPEC-002 — did_cache and revocation_manifests tables.
Body lands in EP-007 (DHT + revocation integration).
"""

# Stub — DID cache queries are implemented in EP-003 M1 via the audit_db schema.
# did_cache and revocation_manifests tables share the same SQLite file.
# Query helpers will be added as needed by L2_dht and L5_captokens.
